import type { NextPage } from "next";
import Head from "next/head";

const Home: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Retro Game App</title>
        <meta name="description" content="Retro Game App" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <main>
        <h1>Retro Game App</h1>
      </main>

      <footer></footer>
    </div>
  );
};

export default Home;
